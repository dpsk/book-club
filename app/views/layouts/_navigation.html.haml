= link_to "Book Club", root_path, :class => 'brand'
%ul.nav
  - if user_signed_in?
    %li
      = link_to "Logged in as #{current_user}", "#"
    %li
      = link_to 'Logout', signout_path, method: :delete
  - else
    %li
      = link_to 'Login', "/auth/google_oauth2"
